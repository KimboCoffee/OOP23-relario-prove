@startuml
package it.unibo.oop.relario {
    package model {
        package furniture {

        }

        package inventory {
            interface Inventory {
                + {method} useItem
                + {method} getItemName
                + {method} getItemDescription
                + {method} dropItem
                + {method} getItemsList
                + {method} addItem
                + {method} getItemsNumber
            }

            interface InventoryItem {
                + {method} getName
                + {method} getDescription
                + {method} getEffect
                + {method} getPower
                + {method} getUsability
            }

            interface Weapon extends InventoryItem{
                + {method} getEffect return damage
            }

            interface Armor extends InventoryItem{
                + {method} getEffect returns protection
            }

            interface Healing extends InventoryItem{
                + {method} getEffect returns healing
                + {method} getUsability returns singleUse
            }

            interface Collectible extends InventoryItem{
                + {method} getEffect returns none
                + {method} getPower returns 0
            }
        }

        package entities {

        }
    }

    package view {
        interface View {

        }
    }

    package controller {
        interface Controller {

        }
    }
}
@enduml